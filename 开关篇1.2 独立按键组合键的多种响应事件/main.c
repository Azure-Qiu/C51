#include "key.h"
#include "smg.h"
/** 
 **  @brief    独立按键组合键的单次响应与连续响应
 **            1. 按键1，按键2同时按下，LED切换为流水灯模式。（按下响应）
 **            2. 按键2，按键3同时按下，LED切换为跑马灯模式。（按下响应）
 **            3. 按键3，按键4同时按下，LED模式速度持续减慢。（连续响应）
 **  @author   QIU
 **  @date     2023.08.31
 **/

/*-------------------------------------------------------------------*/


void main(){
	
	smg_showChar('0', 1, false); // 初始为0
	while(1){
		// 扫描按键
		scan_key();
		// 检查按键
		check_key();
	}
}